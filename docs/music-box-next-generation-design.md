# 新音乐盒完整实现方案

> 状态：设计方案，尚未实现。
>
> 当前已上线接口与行为仍以 [`music-box-api.md`](music-box-api.md) 为准。本文档描述下一代音乐盒的目标架构、完整运行流程、兼容迁移顺序和验收标准，供后续客户端与服务端开发共同使用。

## 1. 目标

在保留现有“服务端音乐机器人向 LiveKit 房间广播单条 Opus 音轨”方案的基础上，安全地增加：

- 上一首、下一首。
- 顺序播放、列表循环、随机播放。
- 可持久保存、编辑和复用的歌单。
- 跨房间复用的音频缓存。
- 控制命令的确定性确认和跨客户端一致状态。
- 服务重启后保留歌单和播放队列，但不擅自恢复声音。
- 能够支撑较长歌单而不逐秒推送完整列表。

本方案不把音乐下载和播放迁移到 Windows、macOS 或 Android 客户端。三个客户端继续只负责搜索、控制、展示和接收 LiveKit 音频，所有播放规则由服务端统一执行。

## 2. 非目标

第一阶段不实现：

- 客户端离线播放或下载歌曲。
- 用户上传本地歌曲文件。
- 跨房间同步播放同一个歌单进度。
- 歌词逐字同步。
- 播放器均衡器或服务端房间总音量。
- 自动恢复服务重启前的播放位置并立即出声。
- 永久保存所有播放过的音频文件。

单曲循环不属于第一版的三个基础模式，但状态机应允许以后增加 `repeat_one`，不需要再次改造队列和缓存。

## 3. 必须保留的现有架构

当前播放链路是：

```text
客户端搜索/点歌
  -> Gang Chat 服务端解析音源
  -> FFmpeg 转码为 48 kHz 双声道 Ogg/Opus
  -> 房间专属 __musicbox__ LiveKit 参与者
  -> 所有已加入语音频道的客户端接收同一条远端音轨
```

这条链路应继续保留，原因包括：

- 房间成员天然听到同一首歌和同一播放进度。
- 音源地址、下载鉴权和转码能力不需要暴露给客户端。
- Windows、macOS、Android 不需要分别维护本地播放器。
- 当前播放器会直接读取 Ogg 中的 Opus packet 并写入 LiveKit，不在播放过程中重复解码、编码。
- 本地听歌音量仍可以由每个客户端单独控制，不改变房间播放状态。

不应改为“服务端只广播播放指令、每个客户端自行拉取歌曲”。这种方案会重新引入音源兼容、重复流量、客户端时钟漂移、后台播放限制和不同设备解码差异。

## 4. 当前实现需要解决的问题

### 4.1 播放完成会消费并删除队列项

当前歌曲自然结束或跳过后，队列记录及其 Ogg 文件都会被删除。该行为不支持上一首、列表循环、随机历史和缓存复用。

新实现必须把以下概念拆开：

- 歌曲元数据。
- 可持久保存的歌单。
- 房间本轮播放队列。
- 播放历史。
- 可随时清理、重新生成的音频缓存。

### 4.2 控制命令可能返回旧状态或被丢弃

当前播放器控制 channel 使用非阻塞写入，队列满时命令会被静默忽略；HTTP handler 在播放器实际处理命令前就可能返回快照。

新实现要求：

- 每条控制命令都有唯一 `command_id`。
- 房间播放状态机按顺序处理命令。
- HTTP 响应必须等待命令应用成功、明确失败或超时。
- 成功响应包含应用后的 `revision` 和权威快照。
- channel 满、播放器关闭、请求超时都必须返回明确错误，不能静默吞掉。

### 4.3 快照可能乱序覆盖

当前客户端会用 HTTP 响应或 SSE 快照整体覆盖本地状态，但快照没有版本号。旧 HTTP 响应或旧 SSE 事件晚到时可能覆盖新状态。

新状态必须带单调递增的 `revision`。客户端仅接受：

- `revision` 大于当前值的完整快照。
- `revision` 等于当前值且 `current_item_id` 匹配的进度事件。
- 旧服务端未提供 `revision` 时，继续使用现有兼容行为。

### 4.4 每秒写数据库并发送完整队列

当前播放心跳每秒保存状态并广播包含完整队列的快照。短队列影响有限，但大型歌单会放大数据库写入和 SSE 流量。

新实现应区分结构事件和进度事件：

- 队列、当前歌曲、播放模式或播放状态变化时发送完整快照。
- 正常播放进度只发送精简进度事件。
- 数据库只在重要状态边界和周期检查点保存，不必每秒写入。

### 4.5 当前排序分配和删除边界不足

队列排序不能继续使用未加事务保护的 `MAX(sort_order) + 固定步长`。并发点歌必须通过事务或房间状态机分配唯一顺序。

所有查询、修改和删除必须同时限定 `room_id` 与 `item_id`，不能只凭全局 item ID 操作记录。

### 4.6 当前文档和代码存在进度同步历史差异

现有部分注释仍描述“客户端本地推进进度”，现行实现则依赖服务端每秒快照。实施新方案时应同时更新现有 API 文档、协议模型注释和测试，避免两套契约并存。

## 5. 目标模块结构

### 5.1 服务端

建议在 `gang-chat-server/internal/musicbox` 内逐步形成以下职责：

```text
manager.go           对外入口、房间引擎生命周期
engine.go            单房间串行播放状态机
selector.go          上一首/下一首/播放模式的纯规则
player.go            LiveKit 连接和 Opus packet 播放
playlist_store.go    歌单和歌单项持久化
queue_store.go       房间播放队列和播放历史持久化
state_store.go       房间播放状态、revision 和恢复点
cache.go             音频缓存索引、租约、LRU 清理
prepare.go           音源解析、下载、转码和预取调度
transcode.go         FFmpeg 适配器
types.go             领域类型和枚举
```

不要求一次性按文件名重写现有实现，但最终职责必须清晰，不能继续让一个队列行同时承担歌曲、历史和缓存文件的生命周期。

### 5.2 客户端

遵守 `modular-development-guide.md` 的依赖边界：

- `lib/src/protocol`
  - 扩展 API、SSE 事件和数据模型。
  - 兼容缺失字段和未知播放模式。
- `lib/src/app`
  - 扩展 `MusicBoxController`。
  - 增加播放模式、控制按钮状态、revision 接收规则等纯逻辑。
  - 不依赖 Flutter、LiveKit 或平台插件。
- `lib/src/home`
  - 渲染上一首、播放/暂停、下一首和模式按钮。
  - 展示歌单、队列、加载、失败和空状态。
  - 只调用 controller，不拼 API body。
- `lib/src/live`
  - 继续只负责识别音乐机器人音轨和设置本地监听音量。
  - 不承担播放顺序、歌单或随机规则。
- `lib/src/ui`
  - 只在确有跨页面复用需求时增加通用歌单/菜单组件。

三端共享同一套协议和业务规则。平台差异只允许出现在响应式布局、输入方式和本地音频输出适配中。

## 6. 领域模型

### 6.1 Track：歌曲元数据

建议建立规范化歌曲表，使用 `(source, external_track_id)` 作为业务唯一键。

主要字段：

- `id`
- `source`
- `external_track_id`
- `title`
- `artist`
- `duration_ms`
- `cover_ref`
- `metadata_version`
- `created_at`
- `updated_at`

客户端提供的歌名、歌手只能作为搜索结果展示和入队初值。服务端在解析音源时应校验并尽量回填权威元数据，同时限制字符串长度。

### 6.2 Playlist：持久歌单

主要字段：

- `id`
- `scope_type`：第一版为 `user`，预留 `room`
- `owner_user_id`
- `room_id`：个人歌单为空，未来房间共享歌单使用
- `name`
- `description`
- `revision`
- `created_at`
- `updated_at`

### 6.3 PlaylistItem：歌单条目

主要字段：

- `id`
- `playlist_id`
- `track_id`
- `sort_order`
- `added_by_user_id`
- `created_at`

重复歌曲允许存在，并计入歌单上限。排序更新必须在事务中完成，并对 `(playlist_id, sort_order)` 建立约束或使用可安全重排的顺序方案。

### 6.4 RoomQueueItem：房间播放队列

歌单是长期模板，房间播放队列是某次播放会话的独立快照。

主要字段：

- `id`
- `room_id`
- `track_id`
- `source_playlist_id`：直接点歌时为空
- `source_playlist_item_id`：直接点歌时为空
- `requested_by_user_id`
- `sort_order`
- `lifecycle`：`queued`、`playing`、`played`、`skipped`、`removed`、`failed`
- `played_sequence`
- `created_at`
- `updated_at`

播放结束后不立即物理删除队列项，而是标记为 `played` 或 `skipped`。这部分记录用于上一首、播放历史和随机模式回退。房间主动清空队列或历史过期时再批量删除。

### 6.5 RoomPlaybackState：房间播放状态

建议在现有状态表上增加：

- `mode`：`sequential`、`repeat_all`、`shuffle`
- `revision`
- `current_queue_item_id`
- `position_ms`
- `state`：继续兼容 `stopped`、`playing`、`paused`
- `shuffle_cycle`
- `shuffle_seed` 或当前洗牌序列标识
- `updated_at`

播放过程中内存状态机是实时权威来源，数据库是恢复点。数据库写入失败不能阻塞 20 ms 的音频写入路径，但必须被记录并重试或降级。

### 6.6 MediaCache：服务端音频缓存

音频文件不属于客户端、不属于歌单，也不属于某个队列项。它是服务端可重建缓存。

缓存键建议为：

```text
source + external_track_id + encoding_profile_version
```

例如：

```text
netease:108485:opus-48k-stereo-fec-v1
```

缓存索引字段包括：

- `cache_key`
- `track_id`
- `encoding_profile`
- `status`：`missing`、`preparing`、`ready`、`failed`
- `file_path`
- `file_size_bytes`
- `duration_ms`
- `checksum`
- `last_accessed_at`
- `failure_count`
- `retry_after`

正在播放或已被预取任务占用的缓存必须持有租约，清理器不能删除。进程崩溃后租约应能过期，不能依赖永久引用计数。

## 7. 存储位置与容量限制

### 7.1 保存位置

- 歌单、歌曲元数据、播放队列、历史和状态：服务端数据库。
- Ogg/Opus 音频：服务端本地磁盘缓存。
- 客户端：不保存完整歌曲，只保存 UI 状态、封面缓存和本地音量偏好。
- 对象存储：第一版不要求；多服务实例部署时可以作为可选共享冷缓存。

音频缓存被清理后，歌单仍然完整。再次播放时服务端重新解析、下载和转码。

### 7.2 默认限制

限制必须由服务端配置和响应能力字段提供，客户端不能硬编码为唯一真值。

建议默认值：

| 项目 | 建议上限 |
|---|---:|
| 单个个人歌单 | 500 首 |
| 单个账号歌单数量 | 50 个 |
| 房间活动播放队列 | 200 首 |
| 迁移期间旧完整快照队列 | 暂时 100 首 |
| 歌单/队列分页大小 | 50 首 |
| 搜索默认返回数量 | 20 首 |
| 预取范围 | 当前歌曲之后 2 首 |

大型歌单只加载元数据，不一次性下载全部歌曲。达到上限时服务端返回稳定错误码，客户端提前显示剩余容量。

## 8. 单房间播放状态机

### 8.1 单一写入者

每个活动房间创建一个 `RoomPlaybackEngine`。该引擎是房间播放状态的唯一写入者，依次处理：

- `enqueue`
- `enqueue_playlist`
- `remove_item`
- `clear_queue`
- `play`
- `pause`
- `resume`
- `previous`
- `next`
- `stop`
- `set_mode`
- `track_ready`
- `track_failed`
- `track_ended`
- `player_disconnected`
- `shutdown`

数据库事务、播放选择和 revision 增长由引擎统一协调。下载、转码和 LiveKit I/O 可以在后台执行，但完成结果必须作为事件重新投递给引擎，不能从后台 goroutine 直接修改房间状态。

### 8.2 命令确认

每个控制命令包含：

- `command_id`
- `room_id`
- `actor_user_id`
- `action`
- 可选 `expected_revision`
- 超时上下文

状态机处理后返回：

- 是否应用成功。
- 新 `revision`。
- 应用后的完整状态或明确错误。

不允许使用 `default` 分支静默丢弃控制命令。高频重复命令可以按 `command_id` 做幂等去重。

### 8.3 基础状态转换

| 当前状态 | 事件 | 结果 |
|---|---|---|
| stopped | play | 准备并播放当前项；无当前项时选择第一项 |
| playing | pause | 保留当前项和位置，停止发送 packet |
| paused | resume/play | 从当前位置继续 |
| playing/paused | next | 按模式选择下一项，保持原先播放/暂停状态 |
| playing/paused | previous | 按上一首规则选择，保持原先播放/暂停状态 |
| playing | track_ended | 按模式选择下一项并继续播放 |
| 任意 | stop | 停止出声，位置归零，保留队列 |
| 任意 | set_mode | 更新模式，不立即切歌 |

如果当前项还没有缓存，状态保持可解释的“正在准备”展示，但协议的基础 `state` 仍兼容现有枚举。可以额外增加 `phase` 字段表示 `idle`、`preparing`、`streaming`、`reconnecting` 或 `failed`。

## 9. 播放模式规则

### 9.1 顺序播放 `sequential`

- 按队列的稳定顺序播放。
- 到最后一首后进入 `stopped`。
- 不因为后面的歌曲先下载完成而越过尚在准备的队首歌曲。
- 当前歌曲明确失败或准备超时后，标记失败并前进到下一首。

### 9.2 列表循环 `repeat_all`

- 到最后一首后回到第一首可播放歌曲。
- 手动 `next` 正常前进。
- 手动 `previous` 按历史回退。
- 单曲队列会持续重复这一首。

### 9.3 随机播放 `shuffle`

使用洗牌袋，不在每次切歌时独立随机：

1. 新一轮开始时对所有有效队列项生成一次随机顺序。
2. 一轮内每首最多播放一次。
3. 一轮结束后生成下一轮顺序。
4. 避免新一轮第一首与上一轮最后一首相同；单曲除外。
5. 新增歌曲加入尚未播放集合，不改动已经播放的历史。
6. 删除歌曲时同时从尚未播放集合移除。
7. `previous` 读取真实播放历史，不重新随机。

洗牌顺序或足以重建它的 seed、cycle 和历史必须持久化，不能在进程重启后产生与已展示状态冲突的新顺序。

### 9.4 预留单曲循环 `repeat_one`

以后增加时规则应为：

- 自然播放结束重复当前歌曲。
- 手动 `next` 和 `previous` 仍切换歌曲。
- 不改变现有三个模式的语义。

## 10. 上一首与下一首

### 10.1 上一首

建议采用常见播放器规则：

- 当前歌曲已播放超过 3 秒：回到当前歌曲开头。
- 当前歌曲不超过 3 秒：返回播放历史中的上一首。
- 没有历史时：回到当前歌曲开头，按钮可以保持可用；完全没有当前项时禁用。
- 随机模式依据历史回退，不能重新随机。
- 暂停状态下切换后仍保持暂停。

阈值应由服务端常量定义，并通过测试固定，不在客户端重复判断。

### 10.2 下一首

- `next` 和旧接口中的 `skip` 保持等价。
- 手动下一首与自然播放结束都进入同一选择函数，但携带不同原因。
- 未来单曲循环中，只有自然结束重复当前项，手动下一首仍前进。
- 当前项被删除时，使用与手动下一首相同的安全释放和选择流程。

## 11. 完整业务流程

### 11.1 搜索歌曲

1. 客户端输入关键词，延续现有防抖和旧请求失效机制。
2. 服务端校验房间访问权限、音源和分页参数。
3. 服务端查询音源，可对短时间相同搜索做小容量 TTL 缓存。
4. 服务端返回规范化 `source`、`external_track_id`、歌名、歌手、时长和封面引用。
5. 搜索不触发音频下载。

### 11.2 添加到个人歌单

1. 客户端提交目标歌单和搜索结果的稳定音源 ID。
2. 服务端验证歌单归属、条目上限和字段长度。
3. 服务端以 `(source, external_track_id)` 获取或创建 Track。
4. 在事务内分配唯一顺序并写入 PlaylistItem。
5. 歌单 revision 加一。
6. 返回更新后的歌单摘要或对应分页。

### 11.3 播放歌单

提供两个明确动作：

- `play`：用歌单替换当前待播队列并开始播放。
- `append`：把歌单内容追加到当前队列末尾。

完整流程：

1. 校验用户是房间成员且有播放控制权限。
2. 校验歌单可访问。
3. 在一个事务中把歌单项复制为 RoomQueueItem。
4. 保留来源歌单和来源条目 ID，用于 UI 标识，但后续编辑歌单不自动改变活动队列。
5. 房间 revision 加一。
6. 状态机选择第一首并启动高优先级准备。
7. 返回命令应用后的完整快照。

### 11.4 直接点歌

1. 客户端提交稳定音源 ID 和幂等键。
2. 状态机串行分配房间队列顺序。
3. Track 已存在时复用；不存在时创建元数据记录。
4. 新队列项进入 `queued`。
5. 如果房间没有当前歌曲，立即选为当前项。
6. 启动当前项和后续两首的预取。
7. 返回新的 revision 和完整快照。

### 11.5 准备音频

1. 根据 Track 和编码 profile 生成 cache key。
2. 缓存命中且文件校验有效：取得租约并立即可播放。
3. 缓存正在准备：加入同一个等待任务，不重复启动 FFmpeg。
4. 缓存缺失：解析临时音源 URL，启动受 worker pool 限制的下载/转码任务。
5. 如果源文件已经是满足要求的 Ogg/Opus，可执行经过验证的快速路径；否则继续完整转码并保留 FEC 参数。
6. 输出先写临时文件，完成校验后原子移动到最终路径。
7. 成功或失败结果作为 `track_ready`/`track_failed` 事件交回房间状态机。
8. 队列项删除或顺序变化时，取消不再需要且无人共享的预取任务。

### 11.6 开始播放

1. 状态机取得当前缓存租约。
2. 确保房间音乐机器人已连接 LiveKit。
3. 打开 Ogg 文件并从头或指定恢复点读取 Opus packet。
4. 写入 LiveKit sample track，并按 packet duration 节流。
5. 更新内存中的 position，不在 20 ms packet 路径执行数据库或 SSE I/O。
6. 播放开始后发布一次完整状态快照。

### 11.7 暂停与恢复

1. 命令进入房间状态机并校验当前 revision。
2. 播放器暂停或恢复 packet 输出。
3. 状态机保存准确 position 和新状态。
4. revision 加一并发布完整快照。
5. HTTP 响应等待上述操作完成后返回。

### 11.8 自然播放结束

1. 播放器只报告 `track_ended(current_item_id)`，不自行查询或删除下一项。
2. 状态机验证事件仍对应当前歌曲，拒绝迟到事件。
3. 当前队列项标记为 `played`，写入播放序号和历史。
4. 选择器按照播放模式返回下一项。
5. 释放旧缓存租约，获取新缓存租约。
6. revision 加一并发布完整快照。
7. 新项目未准备好时等待其高优先级准备，不擅自越过正常队列顺序。

### 11.9 手动上一首/下一首

1. 命令进入状态机。
2. 依据当前 mode、历史、进度和命令原因运行纯选择函数。
3. 播放器停止当前文件读取并切换文件。
4. 原来为 paused 时继续 paused；原来为 playing 时继续 playing。
5. revision 加一，HTTP 返回应用后的新状态。

### 11.10 编辑活动队列

- 删除未播放项：标记为 removed，取消其独占预取，保持当前歌曲。
- 删除当前项：通过统一切歌流程释放资源并选择下一项。
- 移动项目：在事务中重排，只影响尚未播放部分；历史顺序不改。
- 清空队列：停止播放、释放租约并清理本轮队列和历史；不删除歌单和公共缓存。
- 失败项：保留可见错误和重试入口，不让状态机无限重试。

### 11.11 客户端重新连接

1. SSE 断线重连后，客户端调用 `GET /state` 获取完整快照。
2. 客户端用 revision 替换本地状态。
3. 进度事件只有在 room、revision 和 current item 都匹配时才应用。
4. 客户端没有加入 LiveKit 语音时仍可看状态和控制，但不接收音乐音轨。

### 11.12 服务重启

第一版采用保守恢复：

- 保留 Track、Playlist、PlaylistItem、活动队列、历史和播放模式。
- 不删除有效缓存文件。
- 不自动连接音乐机器人，不自动恢复出声。
- 把实时播放状态安全置为 `stopped`，position 置零。
- 可以保留 current item，使用户点击播放后从该曲开头开始；如果该项已被删除则选择下一有效项。
- 清理残留临时文件和过期任务状态。
- 缓存文件缺失时把索引修正为 missing，后续按需重新准备。

这样既不会因为服务重启丢失歌单，又不会在无人确认时突然恢复音乐。

## 12. 状态同步协议

### 12.1 完整快照

保留现有 `music_box_changed` 事件名和完整覆盖语义，增加兼容字段：

```json
{
  "enabled": true,
  "revision": 108,
  "playback": {
    "state": "playing",
    "phase": "streaming",
    "current_item_id": "mbq_123",
    "position_ms": 42000,
    "mode": "shuffle",
    "can_previous": true,
    "can_next": true,
    "updated_at": "..."
  },
  "queue": [],
  "usage": {},
  "limits": {
    "max_playlist_items": 500,
    "max_playlists_per_user": 50,
    "max_queue_items": 200
  }
}
```

结构变化、控制成功、当前歌曲改变、准备失败和重连恢复时发送完整快照。

### 12.2 精简进度事件

新增 `music_box_progress`：

```json
{
  "type": "music_box_progress",
  "room_id": "room_123",
  "revision": 108,
  "current_item_id": "mbq_123",
  "position_ms": 43000
}
```

该事件不包含队列，不触发数据库逐秒写入。第一版仍可每秒发送，之后可以调整为客户端单调计时渲染并每 5～10 秒校准。

客户端不得使用“客户端当前墙上时钟减服务端 updated_at”计算进度。若启用本地平滑推进，应以收到快照时的 `position_ms` 和本机 monotonic Stopwatch 为基准，服务端仍是控制状态的唯一权威。

### 12.3 旧客户端迁移

旧客户端依赖每秒完整快照推进进度。因此发布顺序必须是：

1. 服务端先增加 revision、新字段和精简事件，但继续发送旧完整心跳。
2. Windows、macOS、Android 客户端升级为理解 revision 和精简进度事件。
3. 确认最低可用客户端版本覆盖后，服务端通过配置停止逐秒完整快照。
4. 保留结构变化时的 `music_box_changed`。

不能在新客户端发布前直接关闭旧完整心跳。

## 13. API 设计

### 13.1 保持兼容的现有接口

继续保留：

- `GET /rooms/:room_id/music-box/state`
- `GET /rooms/:room_id/music-box/search`
- `POST /rooms/:room_id/music-box/queue`
- `DELETE /rooms/:room_id/music-box/queue/:item_id`
- `POST /rooms/:room_id/music-box/control`

`skip` 继续作为 `next` 的别名。

### 13.2 新控制动作

```json
{"action":"previous","command_id":"...","expected_revision":108}
```

```json
{"action":"next","command_id":"...","expected_revision":108}
```

```json
{
  "action":"set_mode",
  "mode":"repeat_all",
  "command_id":"...",
  "expected_revision":108
}
```

`expected_revision` 可选。普通播放/暂停可以在旧客户端不传时继续工作；容易覆盖用户编辑的批量操作应要求传入。

### 13.3 个人歌单接口

第一版建议先实现个人歌单：

- `GET /me/music-box/playlists`
- `POST /me/music-box/playlists`
- `GET /me/music-box/playlists/:playlist_id`
- `PATCH /me/music-box/playlists/:playlist_id`
- `DELETE /me/music-box/playlists/:playlist_id`
- `POST /me/music-box/playlists/:playlist_id/items`
- `PATCH /me/music-box/playlists/:playlist_id/items/order`
- `DELETE /me/music-box/playlists/:playlist_id/items/:item_id`

播放到房间：

- `POST /rooms/:room_id/music-box/playlists/:playlist_id/play`
- `POST /rooms/:room_id/music-box/playlists/:playlist_id/append`

未来房间共享歌单可以增加 `/rooms/:room_id/music-box/playlists`，不改变个人歌单表结构。

### 13.4 分页与批量操作

- 歌单列表和歌单项必须分页。
- 批量添加歌单到房间使用一个幂等请求和一个数据库事务。
- 响应不必返回全部 500 个歌单项；返回摘要、revision 和第一页，后续按页获取。
- 房间完整快照在队列较大时也应支持摘要与分页演进，但第一阶段可在 200 首上限内继续返回完整活动队列。

## 14. 错误契约

新增错误码应稳定，不让客户端解析英文 message：

- `playlist_limit_reached`
- `playlist_item_limit_reached`
- `music_box_queue_limit_reached`
- `music_box_revision_conflict`
- `music_box_command_timeout`
- `music_box_busy`
- `music_track_unavailable`
- `music_track_prepare_failed`
- `music_cache_unavailable`
- `music_box_player_reconnecting`

失败响应应说明是否可以重试，并在 revision 冲突时返回最新状态摘要或提示客户端重新拉取。

## 15. 权限和安全

- 房间搜索、直接点歌和播放控制延续当前房间成员权限。
- 个人歌单只有所有者可修改和删除。
- 播放个人歌单到房间时，调用者必须同时拥有歌单访问权和房间成员权限。
- 未来房间共享歌单默认由房间管理员管理，成员是否可编辑应由独立权限决定。
- 所有队列操作按 `(room_id, item_id)` 查询和更新。
- 所有歌单条目操作按 `(owner/scope, playlist_id, item_id)` 查询和更新。
- 不向客户端暴露服务端文件路径、解析后的临时音源 URL、LiveKit token 或第三方鉴权信息。
- 对歌单名、简介、歌名、歌手、音源 ID 和批量项目数量设置长度限制。
- 下载和转码继续使用受控超时、并发上限、临时文件和目标路径清理。

## 16. 缓存与预取策略

### 16.1 优先级

准备任务分为：

1. 当前待播放项：最高优先级。
2. 后续第 1、2 首：普通预取。
3. 其他队列项：不主动下载，除非容量和策略明确允许。

随机模式应根据当前洗牌袋预取，而不是按显示 sort order 预取。

### 16.2 并发去重

相同 cache key 在整个服务进程中只能有一个准备任务。其他房间等待同一个结果。任务完成后分别向等待它的房间状态机发送事件。

### 16.3 清理

缓存清理器按配置周期运行：

- 跳过存在有效租约的文件。
- 优先清理校验失败、长期失败和最久未使用的缓存。
- 达到软上限开始清理，降到目标水位后停止。
- 达到硬上限时拒绝低优先级预取，但当前歌曲仍可尝试准备或返回明确错误。
- 数据库索引存在但文件缺失时修正为 missing。
- 文件存在但无索引时视为孤儿，在安全期后清理。

对象存储只作为未来多实例共享冷缓存选项。即使启用对象存储，本地磁盘仍作为播放热缓存，避免播放循环直接依赖远端对象读取。

## 17. 客户端 UI

### 17.1 基础控制

控制顺序建议为：

```text
上一首 | 播放/暂停 | 下一首 | 播放模式
```

- 播放模式按钮使用已有立体按钮和已有弹出菜单样式。
- 按钮状态由 `lib/src/app` 的纯 display/helper 计算。
- 控制请求进行中只禁用会冲突的按钮，不冻结整个音乐盒。
- 收到更高 revision 后解除 pending 状态。
- 窄屏时允许缩为图标，但必须保留 tooltip、语义标签和触屏可用热区。

### 17.2 歌单入口

后续音乐盒主体可以分为：

- 搜索。
- 当前队列。
- 我的歌单。

歌单列表分页加载，单个歌单进入后显示项目列表、重排、删除、播放和追加操作。不能因为打开 500 首歌单而一次构建或下载全部音频。

### 17.3 进度显示

- 播放控制和歌曲切换只认服务端状态。
- 精简进度事件只更新进度，不覆盖队列。
- 收到不匹配当前歌曲或旧 revision 的进度事件时丢弃。
- SSE 重连、App 从后台恢复或切换房间后重新获取完整状态。

## 18. 可观测性

服务端至少记录或统计：

- 活跃音乐房间数和音乐机器人连接数。
- 控制命令排队时间、处理时间、超时和失败数。
- 当前队列长度、歌单载入数量。
- 搜索耗时与上游错误率。
- 缓存命中率、缓存大小、清理数量。
- 下载/转码耗时、失败率和 worker 使用率。
- LiveKit 发布失败、断开和重连次数。
- SSE 完整快照与进度事件的数量、平均 payload 大小。
- revision 冲突和客户端旧事件丢弃数量。

日志关联字段建议包含 `room_id`、`command_id`、`queue_item_id`、`cache_key` 的不可敏感部分和 `revision`。不得记录临时音源 URL、访问 token 或第三方 cookie。

## 19. 测试方案

### 19.1 服务端纯逻辑测试

`selector.go` 应使用表驱动测试覆盖：

- 空队列、单曲、两首、多首。
- 三种播放模式。
- 自然结束与手动 next。
- 3 秒阈值前后的 previous。
- 暂停时切歌保持暂停。
- 随机一轮不重复。
- 随机跨轮避免首尾立即重复。
- 随机模式新增、删除歌曲。
- 当前歌曲失败或被删除。

### 19.2 服务端状态机测试

- 命令按顺序处理且不会静默丢失。
- HTTP 等待应用后的 revision。
- 迟到的 `track_ended` 不会跳过新歌曲。
- 多人同时点歌顺序唯一且稳定。
- 删除当前项只切换一次。
- 重复 command ID 只应用一次。
- revision 冲突不会覆盖新状态。
- 播放器断开、重连和关闭时释放资源。
- 服务重启保留歌单/队列但不自动出声。

### 19.3 缓存测试

- 相同歌曲并发请求只启动一次转码。
- 不同房间可以共享 ready 缓存。
- 正在播放的缓存不会被清理。
- 预取取消不影响其他等待者。
- 临时文件失败后不会被误认为 ready。
- 缓存索引与磁盘不一致可以自愈。
- LRU 在软/硬上限下行为正确。

### 19.4 服务端数据库测试

继续使用独立 `GANG_TEST_MUSICBOX_MYSQL_DSN`，覆盖：

- 歌单、歌单项和队列事务。
- 最大条目限制。
- 重排与唯一顺序。
- 分页边界。
- 重复项。
- 并发添加。
- 跨房间和跨用户权限隔离。
- 状态 revision 持久化。

### 19.5 客户端单元测试

放在 `test/unit/app` 和 `test/unit/protocol`：

- 新旧快照解析。
- 未知 mode 的安全回退。
- revision 接收/拒绝规则。
- 进度事件匹配规则。
- 控制按钮状态。
- 三种模式文案和图标映射。
- 歌单/队列上限错误映射。
- API body、分页和幂等键。

### 19.6 客户端 widget 测试

放在 `test/widget/home`：

- 宽屏、窄屏下四个控制按钮不 overflow。
- pending、失败、空歌单和长歌名单。
- 上一首/下一首/模式按钮正确调用 controller。
- 快速连续点击不会重复发送或冻结 UI。
- 歌单分页和项目重排。
- Android 触屏、Windows/macOS 鼠标入口。
- 音乐盒关闭、房间切换或账号退出后的异步回调安全。

### 19.7 集成与长时间测试

- 使用真实 LiveKit 连续播放至少 2 小时。
- 100～500 首元数据歌单，活动队列达到上限。
- 播放期间多人同时点歌和切换模式。
- 服务端重启、LiveKit 短暂断开、SSE 重连。
- 缓存接近容量上限时继续播放。
- Windows、macOS、Android 同时在线观察状态和声音。
- Android 进入后台、恢复和重新加入语音频道。

## 20. 实施阶段

### 阶段 0：固定契约

- 确认三种播放模式和上一首规则。
- 确认个人歌单第一版范围和容量限制。
- 更新本文档中的未决项后再开始实现。

### 阶段 1：状态一致性，不改现有 UI

- 增加房间 revision。
- 把控制命令改为有确认的串行状态机。
- 修复命令丢弃和旧快照覆盖。
- 所有 item 操作限定 room ID。
- 保持现有 play/pause/next 行为和接口兼容。

完成标准：旧 UI 无变化，但控制响应始终对应实际已应用状态。

### 阶段 2：拆分缓存和播放历史

- 建立 Track、MediaCache 和租约。
- 队列项不再拥有文件路径。
- 播放完成保留历史，缓存按 LRU 清理。
- 增加当前项和后两首预取。
- 服务启动不再清空歌单和全部缓存。

完成标准：同一首歌重复播放或跨房间播放不重复转码，上一首所需数据可用。

### 阶段 3：播放控制和模式

- 实现纯 selector。
- 增加 previous、sequential、repeat_all、shuffle。
- 客户端增加上一首和模式按钮。
- 保持 Windows、macOS、Android 同一业务行为。

完成标准：模式转换矩阵和长时间播放测试通过。

### 阶段 4：个人歌单

- 建立 Playlist 和 PlaylistItem。
- 增加 CRUD、分页、重排、play 和 append。
- 客户端增加“我的歌单”界面。
- 应用服务端限制和错误提示。

完成标准：歌单在服务重启、客户端退出和跨设备登录后仍可用。

### 阶段 5：同步与性能优化

- 增加精简进度事件。
- 新客户端使用 revision 和进度事件。
- 完成兼容窗口后停止逐秒完整快照和逐秒数据库写入。
- 增加指标和容量告警。

完成标准：长歌单播放时 SSE payload 和数据库写入不随队列长度逐秒放大。

### 阶段 6：可选扩展

- 房间共享歌单。
- 单曲循环。
- 对象存储冷缓存。
- 搜索历史、最近播放和收藏迁移。
- 管理员队列权限和点歌冷却策略。

## 21. 回归风险

主要风险和对应防护：

| 风险 | 防护 |
|---|---|
| 新旧客户端事件契约不同 | 先加字段和事件，保留旧完整心跳，分阶段切换 |
| 状态机阻塞音频线程 | packet 写入与数据库/SSE 分离，完成事件回投状态机 |
| 缓存清理删除播放中文件 | 租约、临时文件、原子重命名和清理测试 |
| 随机/上一首在队列编辑后错乱 | 显式历史和洗牌袋，不从 sort order 猜历史 |
| 并发点歌顺序重复 | 单房间串行分配或数据库事务与唯一约束 |
| 服务重启突然自动播放 | 重启统一进入 stopped，不自动连接机器人 |
| 大歌单扩大 SSE 和 widget 开销 | 队列上限、分页、精简进度事件和惰性列表 |
| Android 修改影响桌面 | 业务规则共享，只有响应式 UI 分支做平台差异测试 |

## 22. 验收清单

- [ ] 当前服务端广播链路和本地音量功能保持不变。
- [ ] 控制命令不会静默丢失，成功响应对应已应用状态。
- [ ] 客户端不会用旧 revision 覆盖新状态。
- [ ] 上一首、下一首和三种模式在三端行为一致。
- [ ] 随机模式一轮内不重复，上一首按历史返回。
- [ ] 暂停状态切歌后仍保持暂停。
- [ ] 同一首歌重复播放不重复转码。
- [ ] 大歌单不会触发全量下载。
- [ ] 歌单保存在数据库，音频只作为服务端缓存。
- [ ] 服务重启后歌单和队列保留，但不会自动出声。
- [ ] 正在播放的缓存不会被清理。
- [ ] 达到歌单/队列上限时返回稳定错误码。
- [ ] SSE 重连后能通过完整快照恢复一致状态。
- [ ] 新旧客户端兼容迁移经过实际版本组合测试。
- [ ] Windows、macOS、Android 的聚焦测试和真实 LiveKit 长时间测试通过。

## 23. 最终原则

后续实现应始终遵守以下边界：

1. 服务端是播放顺序和播放状态的唯一权威。
2. 客户端只做控制、展示和本地监听音量，不自行播放音源。
3. 歌单是持久模板，房间队列是一次播放会话的快照。
4. 音频是可重建缓存，不能与歌单生命周期绑定。
5. 所有房间播放变更经过同一个串行状态机。
6. 所有快照和控制结果带 revision，拒绝旧状态覆盖。
7. 播放 packet 路径不执行数据库、HTTP、SSE 或缓存清理。
8. 大歌单必须分页、按需预取，不能全量下载或逐秒全量广播。
9. 新接口采用向后兼容的增量迁移，不一次破坏现有客户端。
10. 每个阶段先完成状态机和边界测试，再接 UI。
